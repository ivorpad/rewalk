# Same layout npm produced, so swapping the package manager changes who fetches
# the packages and nothing about how anything resolves them. skill/.npmrc says
# why that directory needs it; this one matches so both trees look alike to
# doctor, to CI, and to anyone reading a stack trace.
node-linker=hoisted
